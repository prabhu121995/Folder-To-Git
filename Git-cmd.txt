config git into machine
git config --global user.name "username"
git config --global user.email "username"

GIT-To-Folder
git clone origin-url
git status
git add .
git commit -m "cmd" - move to staging area 
git push origin main- to server
git pull - get the updated words and file from server

Folder-To-GIT
git init - git package
git push origin main - you will get the error here bcoz not able to find the origin
git remote origin origin-url
git branch = its a master (have to change to main)
git branch -M main


Branch
must created in the server ex : Branch2
git branch -a ( for check the list of branch)
git pull (for get the all updated)
git checkout Branch2 (change the branch)
We will do all same process in Branch2
git marge Branch2 (to main)

git branch newBranch (create new branch from local)
push to our branch
then we have to resie the pull request to boss


Conflict
create new two brach in server